<p id="notice"><%= notice %></p>

<div id="myCarousel" class="carousel slide" data-ride="carousel">
  <div class="carousel-inner">
    <div class="carousel-item active">
      <span class="image one" />
      <h1>Brush on the Go</h1>
    </div>
    <div class="carousel-item">
      <span class="image two" />
      <h1>Brush on the Go</h1>
    </div>
    <div class="carousel-item">
      <span class="image three" />
      <h1>Brush on the Go</h1>
    </div>
  </div>
</div>

<!-- Add Filters for Products - Added by Shalini -->
<form class="form-inline search-bar">
  <div class="form-group mb-2">
    <%= form_tag products_path, method: :get do %>
    <%= text_field_tag :query,
    params[:query],
    class: "form-control ",
      placeholder: "Search for a Toothbrush"%>
    </div>
    <% end %>
  </form>
  <!-- End of Filter Addition -->
<div
id="map"
data-markers="<%= @markers.to_json %>"
data-mapbox-api-key="<%= ENV['MAPBOX_API_KEY'] %>"
></div>


  <% @products.each do |product| %>
  <div class="toothbrush-card">
    <div class="background-toothbrush" style="background-image: url(<%= cl_image_path product.photo %>)"></div>
      <h1><i class="far fa-heart heart-icon" id="heart<%= product.id%>"></i></h1>
      <%= link_to product do%>
    <%#= cl_image_tag image.photo, height: 300, width: 400, crop: :fill  %>
    <h6><%= product.title %></h6>
    <p> <%= product.price %> SEK per night</p>
    <div>
    <% product.rating.times do %>
      <i class="fas fa-star stars"></i>
    <% end %>
    </div>
    <br>
  </div>
  <% end %>
  <% end %>
  </div>


  <br>
  <div id="space-for-navbar"></div>
